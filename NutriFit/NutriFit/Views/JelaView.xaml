<UserControl x:Class="NutriFit.Views.JelaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NutriFit.Views"
             mc:Ignorable="d">

    <Grid>
        <Grid>
        <ScrollViewer Margin="0,144,312,104">
            <ItemsControl ItemsSource="{Binding Jela}" Height="492" Width="435" >
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0 0 10 10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Cijena}" FontWeight="Bold" Grid.Row="0"/>

                                <Image Width="16" Height="16" Grid.Row="1">
                                    <Image.Source>
                                        <BitmapImage UriSource="{Binding Slika}" />
                                    </Image.Source>
                                </Image>
                                <TextBlock Text="{Binding Kalorije}" TextWrapping="Wrap" Grid.Row="1"/>
                                <Button Content="Dodaj u korpu" Command="{Binding DodajKorpaCommand}" Grid.Row="1">
                                </Button>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        </Grid>
        <Grid>
        <ScrollViewer Margin="516,140,4,108">
            <ItemsControl ItemsSource="{Binding Jela}" Height="492" Width="183" >
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0 0 10 10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Cijena}" FontWeight="Bold" Grid.Row="0"/>

                                
                                <TextBlock Text="{Binding Kalorije}" TextWrapping="Wrap" Grid.Row="1"/>
                                <Button Content="Dodaj u korpu" Command="{Binding AddCommand}" Grid.Row="1">
                                </Button>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
            
        </Grid>
        <Button Content="Dodaj Jelo"  Command="{Binding OpenNovoJeloWindowCommand}" Margin="276,542,304,24"></Button>
    </Grid>
</UserControl>
